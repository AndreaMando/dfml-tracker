# Copy to .env.local and fill in. See README.md > Environment variables.

POSTGRES_URL=

# Optional — only needed for the leghe.fantacalcio.it results sync (Scores page)
LEGHE_APP_KEY=
LEGHE_AUTH_TOKEN=
LEGHE_COMPETITION_ID=

# Optional — only needed for the fantaasta listone sync (Players page)
FANTAASTA_AUTH_TOKEN=
